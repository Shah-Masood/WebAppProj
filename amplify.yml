version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd webappproj
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: webappproj/build
    files:
      - '**/*'
  cache:
    paths:
      - webappproj/node_modules/**/*

